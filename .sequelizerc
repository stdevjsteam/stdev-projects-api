const path = require("path");

module.exports = {
  config: "./db-config.json",
  "models-path": path.resolve("src", "app", "factory"),
  "seeders-path": path.resolve("src", "db", "seeders"),
  "migrations-path": path.resolve("src", "db", "migrations")
};